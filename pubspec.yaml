name: flutter_jitter
description: 'A Laravel Reverb WebSocket client for Flutter.'
version: 0.0.3
repository: 'https://github.com/jasperfernandez/flutter_jitter'

environment:
  sdk: ^3.7.0
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.2
  http: ^1.3.0
  logger: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.5
  build_runner: ^2.4.15

  async: any
  stream_channel: any
flutter:
